node_modules/
package-lock.json
package.json
.DS_Store
*.tmp

# secrets / env — NEVER commit Supabase keys, service key, deploy hook
.env
.env.*
!.env.example
*.local

# local SQL paste helpers — may contain the deploy hook URL; never commit
supabase/_apply*.sql

# build artifacts (generated from Supabase at build time)
data/storefront.json

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# internal planning docs (keep local only)
docs/superpowers/
